# TeamAgentX 调度系统全场景流程图

> 更新日期：2026-06-15  
> 状态：当前实现说明，以服务端源码为准  
> 范围：Web、桌面端、移动端、Bridge 和定时任务进入同一消息调度链路后的行为

## 1. 核心概念

| 概念 | 说明 |
| --- | --- |
| 智能协作模式 | 服务端值为 `coordinator`。历史值 `auto` 会被归一化为该模式 |
| 手动模式 | 服务端值为 `manual`。用户可通过明确 `@` 或有效默认助手触发；助手消息不会自动接力 |
| 快速对话 | 房间带有 `quickChatAgentId`，无 `@` 时直接交给快速对话助手 |
| 普通业务助手 | 已加入当前群、处于启用状态且 `agentLevel != system` 的助手 |
| 协调器 | 系统级助手，负责输出结构化调度决策，不作为普通业务候选 |
| 独立任务 | 协调器的每条 `assignment` 只发送给一个目标助手 |
| 并行调度 | 多个独立任务同时入队，全部完成后再由协调器汇总 |
| 串行调度 | 每次只入队一个任务，前一步完成后才投递下一步 |
| UI 调度计划 | 协调器在群里展示的整体任务计划，仅用于展示，不再次触发消息路由 |

## 2. 全局总览

```mermaid
flowchart TD
    A["消息进入系统"] --> B{"消息来源"}
    B -->|用户发送| C["Socket 保存并广播消息"]
    B -->|外部平台| D["Bridge 标准化消息"]
    B -->|定时任务| E["创建 isHuman=true 的系统用户消息"]
    B -->|助手输出| F["保存助手消息"]
    B -->|协调器计划| G["仅广播到 UI，不递归触发路由"]

    C --> H["触发 receivedMessage"]
    D --> H
    E --> H
    F --> H

    H --> I["识别发送者、房间模式、回复关系和有效 @"]
    I --> J{"发送者类型"}
    J -->|人类或视为人类| K["用户消息路由"]
    J -->|业务助手| L["助手消息路由"]
    J -->|协调器| M["协调器消息不走普通助手路由"]

    K --> N{"路由结果"}
    L --> N
    N -->|直接执行| O["为单个助手创建队列任务"]
    N -->|请求协调| P["协调器生成结构化决策"]
    N -->|无需处理| Q["结束，不产生助手回复"]
    N -->|等待用户| R["群内提示用户补充信息"]

    P --> S{"协调器决策"}
    S -->|dispatch 单任务| O
    S -->|dispatch 并行| T["启动或合并并行批次"]
    S -->|dispatch 串行| U["启动串行链，仅投递第一步"]
    S -->|ask_owner| R
    S -->|no_dispatch| Q
    S -->|cannot_dispatch| Q

    T --> V["并行批次完成或被用户打断"]
    U --> W["串行链完成、失败或被用户打断"]
    V --> X{"是否需要协调器汇总"}
    W --> X
    X -->|需要| P
    X -->|静默收口| Q
```

## 3. 消息预处理与通用拦截

```mermaid
flowchart TD
    A["收到 receivedMessage"] --> B["加载房间、发送者和群成员"]
    B --> C["归一化触发模式<br/>auto -> coordinator"]
    C --> D{"是否为人类消息"}

    D -->|是| E["重置协作跳转预算和停滞救援计数"]
    E --> F["中止正在运行的看门狗协调请求"]
    F --> G["标记活跃并行批次和串行链为用户已介入"]
    G --> H["进入用户消息路由"]

    D -->|否| I{"助手是否 @ 了人类"}
    I -->|是| J["工作台标记为需要输入"]
    J --> K["不启动停滞看门狗"]
    K --> L{"是否为手动模式"}
    I -->|否| L

    L -->|是| M["助手之间的 @ 仅展示，不继续路由"]
    L -->|否| N{"是否命中当前串行任务"}
    N -->|是| O["消息正常展示<br/>等待队列任务结算推进串行链"]
    N -->|否| P{"是否属于活跃并行批次"}

    P -->|尚有未完成成员| Q["标记当前成员完成并等待"]
    P -->|最后成员完成| R{"批次期间用户是否介入"}
    R -->|否| S["触发 parallelBatchJoin 协调汇总"]
    R -->|是| T["静默关闭批次"]
    P -->|不属于批次| U["进入普通助手消息路由"]
```

### `@` 识别规则

- 只识别当前房间内启用助手的名称。
- 代码块和行内代码中的 `@` 会被忽略。
- 中文连续文本中的 `@助手名` 可以识别。
- 类似邮箱地址的 ASCII 文本不会被误判为助手提及。
- 同一助手被重复提及时只保留一次。
- 长名称优先匹配，避免被短名称截断。

## 4. 用户消息完整路由

```mermaid
flowchart TD
    A["用户消息"] --> B["解析有效助手 @"]
    B --> C{"是否为快速对话房间"}

    C -->|是且无 @| D{"是否配置 quickChatAgentId"}
    D -->|是| E["直接投递快速对话助手<br/>使用会话目录并跳过普通群历史"]
    D -->|否| F["继续普通房间路由"]
    C -->|否| F
    C -->|是且有 @| G["按明确 @ 路由，不自动触发快速助手"]

    F --> H{"是否有有效 @"}
    G --> H

    H -->|有一个 @| I["校验助手启用且属于当前房间"]
    I -->|有效| J["直接投递该助手"]
    I -->|无效| K["结束，不投递"]

    H -->|有多个 @| L{"智能协作普通房间"}
    L -->|是| M["交给协调器<br/>原因 humanMultiMention"]
    L -->|否| N["只取允许的单个目标<br/>前端通常已限制多选"]

    H -->|无 @| O{"上一条消息是否为助手且 @ 当前用户"}
    O -->|是| P["直接回复该助手"]
    O -->|否| Q{"当前消息是否引用回复某个助手"}
    Q -->|是| R["直接回复被引用的助手"]
    Q -->|否| S{"是否配置有效默认助手"}
    S -->|是| T["直接投递默认助手"]
    S -->|否| U{"是否为智能协作模式"}

    U -->|是且存在普通业务助手| V["协调器必须按相关度选出一个助手<br/>原因 humanUnroutedMessage"]
    U -->|是但无普通业务助手| W["协调器使用常规决策空间"]
    U -->|否| X["不响应<br/>前端通常会阻止并提示先指定助手"]

    V --> Y["强制 decision=dispatch"]
    Y --> Z["强制只有一个 assignment"]
    Z --> AA["原样转发用户消息给最相关助手"]

    M --> AB["协调器决定单任务、并行或串行"]
    W --> AB
```

### 无默认助手时的智能协作规则

当用户没有 `@`、没有可用回复目标、没有默认助手，并且房间处于智能协作模式时：

1. 从当前群内所有启用的普通业务助手中选择。
2. 按助手名称、描述和调度规则与用户消息的相关度判断。
3. 必须选择一个助手执行，不允许返回 `no_dispatch`、`ask_owner` 或 `cannot_dispatch`。
4. 只生成一个独立任务。
5. 将用户原始消息完整转发给被选中的助手，避免协调器改写后丢失要求。
6. 只有群内不存在任何普通业务助手时，才回退到协调器的常规决策空间。

## 5. 助手消息完整路由

```mermaid
flowchart TD
    A["业务助手输出消息"] --> B{"是否 @ 人类"}
    B -->|是| C["工作台标记等待用户输入<br/>本条消息不启动看门狗"]
    C --> D{"房间是否为手动模式"}
    B -->|否| D
    D -->|是| E["结束<br/>助手 @ 仅作为展示文本"]
    D -->|否| F["解析除自身外的有效助手 @"]

    F --> G{"提及数量"}
    G -->|0| H["不立即调度其他助手"]
    H --> I["满足条件时启动停滞看门狗"]

    G -->|1| J{"是否为快速对话房间"}
    J -->|是| K["直接投递第一个被提及助手"]
    J -->|否| L["校验目标是否启用且属于当前群"]
    L -->|无效| M["交给协调器<br/>原因 assistantMentionNotMember"]
    L -->|有效| N["登记单助手交接预算"]
    N --> O{"是否超过跳数或形成循环"}
    O -->|否| P["直接投递目标助手"]
    O -->|是| Q["协调器以确定性消息通知群主<br/>本次及后续交接停止"]

    G -->|多个| R{"是否为快速对话房间"}
    R -->|是| S["直接投递第一个被提及助手"]
    R -->|否| T["交给协调器<br/>原因 assistantMultiMention"]

    M --> U["协调器重新判断可执行目标"]
    T --> U
```

### 助手直连交接预算

- 预算只限制“单个助手明确 `@` 另一个助手”的快速交接路径。
- 每次人类发送新消息都会重置预算。
- 限制包含最大交接跳数和同一对助手持续往返形成的循环。
- 超限后不再调用大模型判断，而是由协调器发送确定性的停止提示。
- 协调器主动创建的并行或串行任务不使用该快速交接预算。

## 6. 协调器结构化决策

```mermaid
flowchart TD
    A["触发协调器"] --> B["加载房间规则、消息上下文和候选助手"]
    B --> C["候选助手过滤<br/>启用、已入群、非 system"]
    C --> D["拼接群调度规则和触发原因"]
    D --> E{"触发原因是否为 humanUnroutedMessage<br/>且存在普通业务助手"}

    E -->|是| F["限制工具 Schema<br/>只允许 dispatch"]
    F --> G["assignments 必填且最多一项"]
    G --> H["要求按相关度选择并原样转发"]

    E -->|否| I["使用常规协调工具 Schema"]
    I --> J["允许 dispatch、ask_owner、no_dispatch、cannot_dispatch"]
    J --> K["dispatch 可选择 parallel 或 serial"]

    H --> L["调用 LLM 工具决策"]
    K --> L
    L --> M{"调用或解析是否成功"}
    M -->|否| N["记录跳过并结束"]
    M -->|是| O{"决策类型"}

    O -->|no_dispatch| P["房间空闲时同步工作台为待复核<br/>不发送群消息"]
    O -->|cannot_dispatch| Q["记录原因，不发送群消息"]
    O -->|ask_owner| R["协调器在群内提问<br/>房间空闲时同步工作台"]
    O -->|dispatch| S["解析 assignments"]

    S --> T["按名称或 ID 解析目标助手"]
    T --> U["过滤未启用、未入群和重复目标"]
    U --> V{"是否存在有效任务"}
    V -->|否| N
    V -->|是| W{"humanUnroutedMessage 是否意外产生多项"}
    W -->|是| X["截断为第一项"]
    W -->|否| Y["保留全部独立任务"]
    X --> Z["生成 UI 调度计划"]
    Y --> Z

    Z --> AA{"任务数量和 dispatchMode"}
    AA -->|单任务| AB["直接创建一个队列任务"]
    AA -->|多任务 parallel| AC["同时创建各自的队列任务"]
    AA -->|多任务 serial| AD["创建串行链，仅投递第一项"]
```

### 调度计划与实际任务消息

协调器会先在群内展示完整计划，例如：

```md
**并行任务**

- @前端助手：实现页面和交互
- @后端助手：实现接口和数据模型
```

但实际执行时会拆成两个彼此独立的队列任务：

```text
@前端助手 实现页面和交互
```

```text
@后端助手 实现接口和数据模型
```

这样既能让群内计划清晰可读，也能保证每个助手只接收到自己的任务。串行模式同样拆分任务，只是按顺序逐个投递。

## 7. 并行调度生命周期

```mermaid
flowchart TD
    A["协调器产生多个 parallel assignments"] --> B["群内展示并行任务计划"]
    B --> C["为每个助手立即创建独立队列任务"]
    C --> D["创建或合并房间并行批次"]

    D --> E1["助手 A 执行"]
    D --> E2["助手 B 执行"]
    D --> E3["助手 N 执行"]

    E1 --> F["助手消息进入 receivedMessage"]
    E2 --> F
    E3 --> F

    F --> G["从批次 pending 集合移除当前助手"]
    G --> H{"是否还有未完成成员"}
    H -->|是| I["抑制该消息继续路由<br/>等待其他成员"]
    H -->|否| J{"批次期间用户是否发送过新消息"}

    J -->|否| K["触发 parallelBatchJoin"]
    K --> L["协调器读取各助手结果"]
    L --> M{"是否还需继续调度"}
    M -->|是| N["产生新一轮单任务、并行或串行任务"]
    M -->|否| O["批次正常结束"]

    J -->|是| P["静默关闭批次"]
    P --> Q["不再自动汇总<br/>以用户新消息开启的新路由为准"]
```

### 并行批次规则

- 同一房间已有并行批次时，新并行任务会合并到当前批次，不覆盖未完成成员。
- 批次成员输出中的助手 `@` 不会立即触发新任务，必须等待批次汇合。
- 最后一个成员完成后，只有在用户没有介入时才自动请求协调器汇总。
- 用户在批次期间发新消息，会保留已产生的消息，但关闭原批次的自动汇总。

## 8. 串行调度生命周期

```mermaid
flowchart TD
    A["协调器产生多个 serial assignments"] --> B["群内展示带序号的串行任务计划"]
    B --> C["保存有序步骤和当前索引"]
    C --> D["只为第一步创建队列任务"]
    D --> E["绑定当前 taskQueueId"]

    E --> F["当前助手执行并输出消息"]
    F --> G["消息正常展示<br/>不通过 @ 推进下一步"]
    G --> H["等待队列任务结算"]
    H --> I{"任务结算状态"}

    I -->|failed 或 cancelled| J["清除串行链并终止"]
    I -->|completed| K{"用户是否已介入"}
    K -->|是| L["停止剩余步骤并静默收口"]
    K -->|否| M{"是否还有后续步骤"}

    M -->|有| N["查找下一个有效助手"]
    N --> O{"助手是否启用且仍在群内"}
    O -->|否| P["跳过该步骤并继续查找"]
    P --> N
    O -->|是| Q["携带前序结果创建下一步队列任务"]
    Q --> E

    M -->|无| R["串行链全部完成"]
    R --> S["触发 serialChainJoin"]
    S --> T["协调器汇总或继续生成新任务"]

    N -->|无剩余有效助手| U["终止串行链"]
    Q -->|入队失败| U
    S -->|协调器不可用| U
```

### 串行推进规则

- 助手消息只负责展示结果，不负责推进链路。
- 队列任务的 `completed`、`failed` 或 `cancelled` 结算事件才是唯一推进依据。
- 下一步会获得之前助手的最终输出作为历史上下文。
- 后续助手已停用或离开群时会被跳过。
- 用户插话后，当前步骤可正常结算，但剩余步骤不再自动执行。

## 9. 停滞看门狗

```mermaid
flowchart TD
    A["智能协作普通房间中的业务助手输出"] --> B{"消息是否 @ 人类"}
    B -->|是| C["不安排看门狗"]
    B -->|否| D["按房间启动防抖计时器"]

    D --> E["计时到期后重新检查房间状态"]
    E --> F{"仍为智能协作普通房间"}
    F -->|否| G["结束"]
    F -->|是| H{"是否存在 pending 或 executing 任务"}
    H -->|是| G
    H -->|否| I{"最新消息仍是业务助手且未 @ 人类"}
    I -->|否| G
    I -->|是| J{"连续救援次数是否未超限"}
    J -->|否| G
    J -->|是| K{"协调器是否可用"}
    K -->|否| G
    K -->|是| L["触发 stallWatchdog 协调决策"]
    L --> M["记录本次救援派发的助手"]

    N["用户发送新消息"] --> O["取消计时器和协调器请求"]
    O --> P["中止看门狗派发的执行中任务"]
    P --> Q["取消看门狗派发的待执行任务"]
    Q --> R["重置救援计数"]
```

看门狗只处理“助手说完后房间空闲，但协作可能还没真正结束”的情况。它不会在仍有队列任务、助手正在等待用户输入或救援次数已超限时继续调度。

## 10. 用户介入与停止任务

```mermaid
flowchart TD
    A{"用户操作"} -->|发送新消息| B["标记并行批次和串行链已介入"]
    A -->|停止 pending 任务| C["将任务标记为 cancelled"]
    A -->|停止 executing 任务| D["中止执行器"]

    B --> E["中止看门狗协调请求及其派发任务"]
    B --> F["重置协作交接预算和救援计数"]
    B --> G["原并行批次完成后不再自动汇总"]
    B --> H["原串行链当前步后不再投递剩余步骤"]
    B --> I["新消息按正常用户路由重新决策"]

    C --> J{"任务是否属于串行链"}
    J -->|是| K["清除对应串行链"]
    J -->|否| L["保持其他无关任务状态"]

    D --> M["取消停滞看门狗"]
    M --> N["清除该房间串行链"]
    N --> O["等待执行器和队列状态完成收口"]
```

## 11. 定时任务与 Bridge 消息

```mermaid
flowchart TD
    A{"外部入口"} -->|定时任务| B["创建系统用户消息<br/>isHuman=true"]
    A -->|Bridge| C["适配器标准化文本、提及和来源信息"]

    B --> D{"是否指定 agentIds 或全部助手"}
    D -->|是| E["为每个有效目标分别生成一条单 @ 消息"]
    D -->|目标无效| F["回退到群内全部非系统助手"]
    D -->|未指定目标| G["生成无 @ 消息"]

    E --> H["逐条广播并触发 receivedMessage"]
    F --> H
    G --> H
    C --> H

    H --> I["统一按人类消息路由"]
    I --> J{"是否有明确 @"}
    J -->|是| K["直接目标或多 @ 协调"]
    J -->|否| L["按快速助手、回复关系、默认助手、智能协调顺序处理"]
```

定时任务消息末尾会附带任务名称。Bridge 消息保留平台来源信息，并在创建助手队列任务时继续向下传递。

## 12. 全场景决策表

| 场景 | 调度结果 |
| --- | --- |
| 快速对话，无 `@` | 直接执行 `quickChatAgentId` |
| 快速对话，有 `@` | 执行明确提及的第一个助手 |
| 用户单 `@` 有效助手 | 直接执行该助手 |
| 用户多 `@`，智能协作普通房间 | 交给协调器拆分为单任务、并行或串行 |
| 用户无 `@`，上一条助手明确 `@` 用户 | 直接回复上一条助手 |
| 用户引用回复助手消息 | 直接回复被引用助手 |
| 用户无 `@`，有有效默认助手 | 直接执行默认助手 |
| 用户无 `@`、无默认助手、智能协作、有业务助手 | 协调器按相关度强制选择一个助手 |
| 用户无 `@`、无默认助手、智能协作、无业务助手 | 协调器使用常规决策，可能不派发或询问用户 |
| 用户无 `@`、无默认助手、手动模式 | 不执行；前端通常提前提示 |
| 助手无 `@` | 不立即转交；必要时由停滞看门狗救援 |
| 助手单 `@` 有效群成员 | 在预算允许时直接交接 |
| 助手单 `@` 无效或非群成员 | 交给协调器修正目标 |
| 助手多 `@` | 交给协调器决定并行或串行 |
| 助手只 `@` 人类 | 等待用户输入，不启动看门狗，也不产生助手交接 |
| 助手同时 `@` 人类和助手 | 标记等待用户输入，同时继续处理其中的助手交接 |
| 手动模式下助手 `@` 助手 | 仅展示，不继续执行 |
| 并行最后一个成员完成且用户未介入 | 协调器汇总 |
| 并行期间用户介入 | 批次静默关闭，不自动汇总 |
| 串行当前步骤完成且用户未介入 | 投递下一步，尾部再由协调器汇总 |
| 串行步骤失败或取消 | 终止整条串行链 |
| 串行期间用户介入 | 当前步后停止剩余步骤 |
| 协调器请求失败或结构化结果无法解析 | 记录后结束，不创建任务 |
| 协调器目标停用、离群或重复 | 过滤；无有效目标时结束 |
| 代码中的 `@助手` | 不触发 |
| 定时任务指定多个助手 | 为每个助手创建独立的单 `@` 消息 |
| Bridge 用户消息 | 与普通用户消息进入同一调度流程 |

## 13. 必须保持的调度不变量

1. 协调器的每个 `assignment` 只能描述一个助手的独立任务。
2. 并行与串行是任务之间的执行关系，不应把多个助手的要求塞进同一任务消息。
3. 群内完整调度计划只用于展示，不能再次触发 `receivedMessage`，否则会重复调度。
4. 智能协作模式下，用户无路由目标但群内有业务助手时，必须按相关度选出一个助手。
5. 并行任务必须等待全部成员汇合后再继续协调，除非用户已经介入。
6. 串行任务必须由队列结算事件推进，不能依赖助手输出文本中的 `@`。
7. 用户新消息具有中断旧协作链的优先级，旧批次和旧串行链不得覆盖用户的新意图。
8. 默认助手、快速对话助手和明确回复目标均属于确定性路由，优先于协调器推理。
9. 只有当前房间内启用的助手才能成为最终执行目标。
10. 协调器自身不得出现在普通业务助手的相关度候选列表中。

## 14. 主要源码位置

| 职责 | 文件 |
| --- | --- |
| 消息总路由 | `server/src/core/agent/agent-handler/handler.ts` |
| `@` 解析与消息工具 | `server/src/core/agent/agent-handler/message-utils.ts` |
| 协调器结构化决策 | `server/src/core/agent/coordinator-dispatch.ts` |
| 并行批次跟踪 | `server/src/core/agent/agent-handler/parallel-batch-tracker.ts` |
| 串行链跟踪 | `server/src/core/agent/agent-handler/serial-chain-tracker.ts` |
| 串行任务结算推进 | `server/src/core/agent/agent-handler/task-lifecycle.ts` |
| 停滞看门狗 | `server/src/core/agent/agent-handler/stall-watchdog.ts` |
| 助手交接预算 | `server/src/core/agent/agent-handler/collaboration-budget.ts` |
| 定时任务消息入口 | `server/src/core/cron/cron-scheduler.service.ts` |
| Bridge 消息入口 | `server/src/modules/bridge/bridge.service.ts` |
| Socket 消息入口与停止任务 | `server/src/socket/index.ts` |

## 15. 阅读顺序建议

排查某条消息为什么没有触发助手时，按以下顺序检查：

1. 消息是否真正触发了 `receivedMessage`。
2. 房间是快速对话、智能协作还是手动模式。
3. `@` 是否被识别，目标是否启用并属于当前群。
4. 是否命中快速助手、回复关系或默认助手等确定性路由。
5. 是否进入协调器，以及协调器的触发原因。
6. 协调器是否生成有效 `assignments`，目标是否在过滤后仍有效。
7. 任务属于单任务、并行批次还是串行链。
8. 用户是否在执行期间发送新消息或停止任务。
9. 是否存在交接预算、看门狗次数、任务失败或协调器不可用等终止条件。
